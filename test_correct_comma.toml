# Test file with correct commas in array
title = "Correct Comma Test"

[database]
servers = [
  "server1.example.com",
  "server2.example.com",
  "server3.example.com"
]

# This should pass validation because all strings are separated by commas
